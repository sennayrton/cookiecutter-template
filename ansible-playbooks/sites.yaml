---

#levantamos semaphore sobre docker-compose en local
- hosts: localhost
  become: true
  become_method: sudo
  roles:
    - semaphore

#levantamos locust en local para load-test del frontend
- hosts: localhost
  become: true
  become_method: sudo
  roles:
    - locust

#usando el inventario
- hosts: setup
  become: true
  become_method: sudo
  roles:
    - common
    
#usando el inventario hosts.ini
- hosts: rke2_cluster
  become: true
  become_method: sudo
  roles:
    - common
  
- hosts: registry
  become: true
  become_method: sudo
  roles:
    - registry
